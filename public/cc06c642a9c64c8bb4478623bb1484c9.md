IndexNow verification key

This file explains the purpose of the verification file `cc06c642a9c64c8bb4478623bb1484c9.txt` located at the site root.

Purpose
- The file `cc06c642a9c64c8bb4478623bb1484c9.txt` is used to verify domain ownership for IndexNow (Bing Webmaster Tools) and other search engines that support the IndexNow protocol.
- It must be hosted at the root of your website so it is reachable at: `https://<your-domain>/cc06c642a9c64c8bb4478623bb1484c9.txt`.

Contents
- The `.txt` file must contain the single verification key string:

```
cc06c642a9c64c8bb4478623bb1484c9
```

Notes
- Save the `.txt` file as UTF-8 encoded plain text (no BOM).
- Verification typically completes once the webmaster tool detects the file at the exact path containing the exact key string.
- After verification, you may keep the file or remove it; keeping it does not affect site functionality.

Reference
- IndexNow documentation: https://www.bing.com/indexnow/getstarted

Created by the project maintainers to support IndexNow API verification.