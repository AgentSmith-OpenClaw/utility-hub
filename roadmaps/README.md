# Roadmaps

Long-running build plans for major sections of the site. One file per section.

- [Finance Calculators](./finance-calculators.md) — next calculators ranked by RPM × volume × effort.
- [PDF Tools](./pdf-tools.md) — new `/pdf` section, all client-side, 14 tools in 30 days.
- [Image Tools](./image-tools.md) — new `/image` section, all client-side, 16 tools in 30 days.

Each roadmap is structured as **tiers** (build first → niche) plus a **sequencing plan** and **SEO leverage notes**. Update tiers as items ship; don't delete shipped entries — strike them through so the file doubles as a build log.
